<!--components/like/index.wxml-->
<view class="container" bind:tap="handleChange">
  <image src="{{like_flag?likeImg:dislikeImg}}" class="like_image"/>
  <text class="like_count">{{like_count}}</text>
</view>
